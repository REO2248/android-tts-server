# Copy this file to keystore.properties (ignored by git) and fill values for signing
# Example:
# storeFile=/home/you/.android/release-keystore.jks
# storePassword=change_me
# keyAlias=release_key
# keyPassword=change_me

storeFile=
storePassword=
keyAlias=
keyPassword=
